# SH Lamp iOS Codemagic Build Fix 1

Fixes:
- Swift exclusive-access compile error in `AppViewModel.swift` during BLE disconnect handling.
- Non-fatal redundant `await` warning in `LocalLampController.swift`.

Extract this ZIP into the repository root and replace existing files.
